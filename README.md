Install vivado and add the default fpga .
Create a new project and add the files.
Add verilog and testbench code for sequential multiplier.
Add xdc in constraints files.
Run the tcl commands present in run.tcl for getting netlist.
Run commands.tcl for bitstream generation.
Bitstream generation will ask for hardware fpga connection.
